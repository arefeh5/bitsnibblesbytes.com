# Email Configuration for cPanel Email Service
# Replace these values with your actual cPanel email settings

# SMTP Settings for your cPanel email
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=contact@yourdomain.com
SMTP_PASS=your_email_password

# Email addresses
FROM_EMAIL=contact@yourdomain.com
TO_EMAIL=contact@yourdomain.com

# Common cPanel email settings:
# - SMTP_HOST: Usually mail.yourdomain.com or your server IP
# - SMTP_PORT: 587 (TLS) or 465 (SSL) or 25 (non-secure)
# - SMTP_SECURE: false for port 587, true for port 465